apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  namespace: default
  labels:
    app: test-pod
  annotations:
    secretless.externalsecrets.com/secretstore: "vault-backend"
    secretless.externalsecrets.com/env-vars: "test-app/config"
    secretless.externalsecrets.com/file-secrets: "test-app/config=/mnt/secrets/config.json"
spec:
  serviceAccountName: default
  containers:
  - name: app
    image: busybox
    command: ["sh", "-c", "while true; do env | grep API; cat /mnt/secrets/config.json; sleep 10; done"]
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
      requests:
        cpu: 50m
        memory: 32Mi
